@page
@using TennisDb
@model TennisBruck.Pages.Championship

@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8"/>
    <title>Vereinsmeisterschaft Bruck</title>
    <script src="https://code.jquery.com/jquery-1.8.3.min.js"></script>
    <script src="https://underscorejs.org/underscore-min.js"></script>
    <link rel="stylesheet" href="~/css/championship/app.css"/>
    <link rel="stylesheet" href="~/css/tournament.css"/>
    <script src="js/toggleVisibility.js"></script>
    <script src="js/tournament.js"></script>
</head>
<body>
<div class="championship-container">
    <header class="championship-header">
        <i class="icon">🏆</i>
        <h1>Vereinsmeisterschaft Bruck</h1>
        <i class="icon">🏆</i>
    </header>

    <main>
        <!-- Competition Selector -->
        <section class="competition-selector">
            <form method="post" asp-page-handler="CompetitionChanged">
                <div class="select-wrapper">
                    <select class="competition-select" name="selectedCompetitionId" onchange="this.form.submit()">
                        <option value="">Bewerb auswählen</option>
                        @foreach (var competition in Model.Competitions)
                        {
                            <option value="@competition.Id"
                                    selected="@(Model.SelectedCompetition?.Id == competition.Id)">
                                @competition.Name
                            </option>
                        }
                    </select>
                </div>
            </form>

            @if (Model.SelectedCompetition != null)
            {
                <div class="competition-actions">
                    <h3>@Model.SelectedCompetition.Name</h3>
                    <form method="post">
                        <input type="hidden" name="selectedCompetition" value="@Model.SelectedCompetition.Id"/>
                        @if (Model.IsRegistered)
                        {
                            <button class="registration-button unregister" asp-page-handler="Unregister">Abmelden
                            </button>
                        }
                        else
                        {
                            <button class="registration-button register" asp-page-handler="Register">Anmelden</button>
                        }
                    </form>
                </div>
            }
        </section>

        <!-- Group Management -->
        <section class="groups-section">
            <div class="section-header">
                <h2><i class="icon">👥</i> Gruppen</h2>
                @if (Model.SelectedCompetition != null)
                {
                    <form method="post">
                        <button class="add-group-button" asp-page-handler="CreateGroup">➕ Gruppe erstellen</button>
                    </form>
                }
            </div>

            <div class="group-container">
                @foreach (var group in Model.Groups)
                {
                    <div class="group-card">
                        <div class="group-header">
                            <h3>@group.GroupName</h3>
                            <div class="group-actions">
                                <form method="post">
                                    <input type="hidden" name="groupId" value="@group.Id"/>
                                    <button class="icon-button increase" asp-page-handler="IncreaseGroupSize">➕</button>
                                    @if (group.MaxAmount > group.GroupPlayers.Count)
                                    {
                                        <button class="icon-button decrease" asp-page-handler="DecreaseGroupSize">⬇️
                                        </button>
                                    }
                                    <button class="icon-button delete" asp-page-handler="DeleteGroup">🗑️</button>
                                </form>
                            </div>
                        </div>

                        <table class="player-table">
                            <thead>
                            <tr>
                                <th>Spieler</th>
                                <th>Punkte</th>
                            </tr>
                            </thead>
                            <tbody>
                            @for (int i = 0; i < group.MaxAmount; i++)
                            {
                                try
                                {
                                    var player = group.GroupPlayers[i].Player;
                                    var doublePlayer = group.Competition.PlayerCompetitions
                                        .FirstOrDefault(pc => pc.Player.Id == player.Id)?.DoublePlayer;
                                    <tr>
                                        @if (doublePlayer == null)
                                        {
                                            <td>@(i + 1). @player.ToString()</td>
                                        }
                                        else
                                        {
                                            <td>@(i + 1). @player.ToString() / @doublePlayer.ToString()</td>
                                        }
                                        @if (doublePlayer == null && !group.Competition.IsSingle)
                                        {
                                            <td colspan="3">
                                                <form method="post" asp-page-handler="AddDoublePlayer">
                                                    <input type="hidden" name="groupId" value="@group.Id"/>
                                                    <input type="hidden" name="competitionId"
                                                           value="@group.Competition.Id"/>
                                                    <input type="hidden" name="playerId" value="@player.Id"/>
                                                    <select class="player-select" name="doublePlayerId"
                                                            onchange="this.form.submit()">
                                                        <option value="">Spieler auswählen</option>
                                                        @foreach (var registeredPlayer in Model.RegisteredCompetitionPlayers.Where(p => p.GroupPlayers.All(gp => gp.GroupId != group.Id)))
                                                        {
                                                            @if (Model.DoublePlayers.Any(x => x != null && x.Id == registeredPlayer.Id)) continue;
                                                            <option
                                                                value="@registeredPlayer.Id">@registeredPlayer.ToString()</option>
                                                        }
                                                    </select>
                                                </form>
                                            </td>
                                        }
                                        <td>@group.GroupPlayers[i].Points</td>
                                        @if (Model.CurrentPlayer.IsAdmin)
                                        {
                                            <td>
                                                <form method="post">
                                                    <input type="hidden" name="groupId" value="@group.Id"/>
                                                    <input type="hidden" name="playerId" value="@player.Id"/>
                                                    <input type="hidden" name="doublePlayerId"
                                                           value="@doublePlayer?.Id"/>
                                                    <button class="icon-button delete"
                                                            asp-page-handler="RemovePlayerFromGroup">❌
                                                    </button>
                                                </form>
                                            </td>
                                        }
                                    </tr>
                                }
                                catch (ArgumentOutOfRangeException)
                                {
                                    <tr>
                                        <td colspan="3">
                                            <form method="post" asp-page-handler="AddPlayerToGroup">
                                                <input type="hidden" name="groupId" value="@group.Id"/>
                                                <input type="hidden" name="competitionId"
                                                       value="@group.Competition.Id"/>
                                                <select class="player-select" name="playerId"
                                                        onchange="this.form.submit()">
                                                    <option value="">Spieler auswählen</option>
                                                    @foreach (var player in Model.RegisteredCompetitionPlayers.Where(p => p.GroupPlayers.All(gp => gp.GroupId != group.Id)))
                                                    {
                                                        @if (Model.DoublePlayers.Any(x => x != null && x.Id == player.Id)) continue;
                                                        <option value="@player.Id">@player.ToString()</option>
                                                    }
                                                </select>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            }
                            </tbody>
                        </table>
                    </div>
                }
            </div>

            @if (Model.SelectedCompetition != null)
            {
                <form method="post" class="save-groups">
                    <button class="save-button" asp-page-handler="SaveGroups">💾 Diese Gruppen Einteilung speichern
                    </button>
                </form>
            }
        </section>

        <!-- Matches -->
        <section class="matches-section">
            <h2><i class="icon">📅</i> Deine Spiele</h2>
            <div class="matches-list">
                @foreach (var match in Model.PersonalMatches)
                {
                    <div class="match-card">
                        <form method="post">
                            <input type="hidden" name="matchId" value="@match.Id"/>
                            <div class="match-header">
                                <span class="match-players">@match.Player1 vs @match.Player2</span>
                                <span class="match-competition">(@match.Group.Competition.Name)</span>
                            </div>

                            @if (match.Sets.Count == 0)
                            {
                                <div class="match-input">
                                    <input type="text" name="score" class="score-input"
                                           placeholder="Format x:x x:x usw."/>
                                    <button class="save-score" asp-page-handler="SaveMatch">Spielstand speichern
                                    </button>
                                </div>
                            }
                            else
                            {
                                <div class="match-result">
                                    <span class="sets">@match.SetToString()</span>
                                    @if (match.Player1.Id == Model.CurrentPlayer.Id || match.Player2.Id == Model.CurrentPlayer.Id || Model.CurrentPlayer.IsAdmin)
                                    {
                                        <button class="delete-result" asp-page-handler="DeleteMatch">Ergebnis löschen
                                        </button>
                                    }
                                </div>
                            }
                        </form>
                    </div>
                }
            </div>
        </section>

        <!-- Tournament Bracket -->
        <section class="tournament-section">
            <h2>K.O Phase</h2>
            <div class="tournament-controls">
                <div class="add-bracket" id="add">Finale erstellen</div>
            </div>
            <div class="brackets" id="brackets"></div>
        </section>

        <!-- Info Section -->
        <section class="info-section">
            <button type="button" class="info-toggle" onclick="toggleInfo()">⬇️ Mehr Infos</button>
            <div id="infoBox" class="info-content" style="display: none;">
                <div class="registered-players">
                    <h3><i class="icon">👤</i> Angemeldete Spieler</h3>
                    <table class="players-table">
                        @foreach (var player in Model.RegisteredCompetitionPlayers)
                        {
                            <tr>
                                <td>@player.ToString()</td>
                                @if (Model.CurrentPlayer.IsAdmin)
                                {
                                    <td>
                                        <form method="post">
                                            <input type="hidden" name="playerId" value="@player.Id"/>
                                            <button class="icon-button delete"
                                                    asp-page-handler="RemovePlayerFromCompetition">🗑️
                                            </button>
                                        </form>
                                    </td>
                                }
                            </tr>
                        }
                    </table>
                </div>

                @if (Model.CurrentPlayer.IsAdmin)
                {
                    <div class="admin-controls">
                        <form method="post" class="competition-creation">
                            <input type="text" name="competitionName" class="competition-input"
                                   placeholder="Neuer Bewerb Name"/>
                            <button class="create-competition" asp-page-handler="CreateCompetition">➕ Bewerb erstellen
                            </button>
                        </form>

                        @if (Model.SelectedCompetition != null)
                        {
                            <form method="post">
                                <input type="hidden" name="competitionId" value="@Model.SelectedCompetition.Id"/>
                                <button class="delete-competition" asp-page-handler="DeleteCompetition">🗑️ Diesen Bewerb
                                    löschen
                                </button>
                            </form>
                        }
                    </div>
                }
            </div>
        </section>
    </main>

    <footer class="championship-footer">
        <form method="post">
            <button class="back-button" asp-page-handler="Back">Zurück</button>
        </form>
        <label>@Model.Message</label>
    </footer>
</div>
</body>
</html>
